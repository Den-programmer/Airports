export const isValuesValidation = (username: string, password: string) => username && password
export const isLengthNameValidation = (length: number) => length > 2
export const isLengthPassValidation = (length: number) => length > 6